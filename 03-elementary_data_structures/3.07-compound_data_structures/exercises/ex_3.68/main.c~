#include <stdio.h>
#include <stdlib.h>
#include "link.h"

int main(void)
{
  int n;
  link **multi;
  
  printf("Enter the size of the matrix: ");
  scanf("%d", &n);
  printf("Enter values: ");
  multi = read_multi(n);
  print_list(multi[0]);
  return 0;
}
